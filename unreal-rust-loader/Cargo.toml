[package]
name = "unreal-rust-loader"
version = "0.1.0"
edition.workspace = true

[dependencies]
unreal-ffi = { path = "../unreal-ffi" }
libloading = "0.9"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[lib]
crate-type = ["cdylib"]
